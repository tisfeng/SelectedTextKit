# 执行计划

执行计划记录跨越多个模块、风险或验证阶段的工作。

- 纯 planning 工作只保留在对话中。
- 获准的 implementation 从 `templates.md` 开始。
- 进行中的计划放在 `active/`，完成计划放在 `completed/`。
- 带日期文件统一使用 `YYYY-MM-DD-<kebab-case-slug>.md`。
- 记录范围、授权、Git 快照、风险、进度、决策、验证和完成条件。
- 已完成或已放弃工作不得留在 `active/`。
- 新建或修改的仓库治理文档使用中文。
