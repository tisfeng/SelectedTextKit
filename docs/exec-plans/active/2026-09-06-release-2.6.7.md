# 发布 2.6.7

- 状态：active
- 创建日期：2026-09-06
- 负责人：Codex
- 关联 Issue/PR：[PR #10](https://github.com/tisfeng/SelectedTextKit/pull/10)

## 背景

`main` 已合入 Text Marker selected-text fallback。需要发布补丁版本，并确保 Swift 与
Objective-C 调用方读取到的库版本与发布标签一致。

## 任务摘要

- 意图模式：implementation
- 交付授权：push
- 安全状态：normal
- 目标结果：发布 `2.6.7`，使用 GitHub 默认生成的 Release Notes。
- 允许修改路径：`Sources/SelectedTextKit/Accessibility/AXManager.swift`、
  `Sources/SelectedTextKit/Core/SelectedTextKit.swift`、`SelectedTextKitExampleTests/`、
  `docs/exec-plans/`、`docs/histories/`
- 同任务 history：`docs/histories/2026-09/2026-09-06-release-2.6.7.md`
- 禁止动作：不改动 README 的依赖兼容下限；不手写或覆写 Release Notes。
- 预期交付物：本地提交、远端 `main`、`2.6.7` 标签和 GitHub Release。
- 验收标准：版本元数据、提交、标签和 Release target 一致；Release Notes 由 GitHub 默认生成。

## 写入前状态

- 写入前检查：pass
- 自动提交资格及原因：eligible；用户明确要求执行完整发布。
- 初始 HEAD：`c4db5ff2dfc7d796047d5d0fdf920f61b7b60cfe`
- 初始 staged 路径：无
- 初始 unstaged 路径：无
- 初始 untracked 路径：无
- 初始冲突：无
- Agent-owned paths：本计划列出的允许修改路径。

## 工作计划

1. 将 `STKInfo.version` 更新为 `2.6.7`。
2. 修复审查发现的旧 SDK 常量兼容性，并为 Text Marker 回退补充确定性测试。
3. 运行格式、Swift target build 与 Xcode 测试验证，并记录可复现的环境限制。
4. 审查最终变更、提交发布准备内容，并推送 `main`。
5. 在该提交创建并推送轻量 `2.6.7` 标签。
6. 以 `2.6.6` 为起始标签创建 GitHub 默认生成的 Release Notes，验证线上结果。

## 风险与决策

- `STKInfo.version` 是调用方可读取的版本信息，必须与标签一致。
- README 中的 `from: "2.0.0"` 是兼容版本下限，并非当前发布版本，保持不变。
- GitHub Release 仅使用 `--generate-notes --notes-start-tag 2.6.6`，避免发布日志格式漂移。
- Text Marker SDK 常量在部分受支持 SDK 中不存在，改用等值字符串以保持编译兼容。

## 进度

- [x] 记录发布范围与初始 Git 状态。
- [x] 更新库版本元数据。
- [x] 接收独立审查与测试覆盖评估，发现旧 SDK 兼容性和回退覆盖缺口。
- [x] 实施最小兼容性修复并补充确定性回归测试。
- [ ] 完成验证、增量审查、提交和发布。

## 验证

- `git diff --check`：通过。
- `swift build --target SelectedTextKit`：通过；保留既有 entitlements resource 与 Swift 6
  `AXError` import warning。
- 定向 Xcode 测试（禁用签名）：`AXManagerTextMarkerFallbackTests` 7/7 通过。
- 整套 Xcode 测试（禁用签名）：2/11 通过；9 项与前台应用、AppleScript 和共享剪贴板
  相关用例在 XCTest runner abort 后失败，不作为本次确定性回归测试的结果。
- 独立 reviewer：旧 SDK 常量兼容性与回退覆盖 findings 均已关闭；未发现新增发布阻塞。
- 真实浏览器 Text Marker 回退：未在本机 GUI/辅助功能环境执行，保留为运行时冒烟验证边界。

## 完成条件

- 验证完成或记录明确的环境限制。
- 提交已推送到 `main`，标签和 GitHub Release 指向同一提交。
- Release Notes 保持 GitHub 默认生成格式。
